/**
 * Mock for Nuxt auto-imports (#imports).
 * Re-exports Vue reactivity primitives that Nuxt normally auto-imports.
 */
export { ref, readonly, computed, reactive, watch, watchEffect, toRef, toRefs, unref, isRef } from 'vue'
